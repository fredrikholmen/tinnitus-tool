services:
  - type: web
    name: tinnitus-tool
    env: node
    buildCommand: |
      pnpm install
      pnpm build
    startCommand: pnpm start
    envVars:
      - key: NODE_ENV
        value: production
